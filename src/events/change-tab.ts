import Camera from '../models/camera';
import Tab from '../enums/tab.enum';

export default function changeTab(camera: Camera, canvas: HTMLCanvasElement, tab: Tab): void {
  // switch (tab) {
  //   case
  // }
}